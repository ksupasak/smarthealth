class Mibfs {
  static String parse(List<int>? bytes) {
    String weight = '';
    // if (bytes != null) {
    //   int i = 256 * bytes[bytes.length - 1] + bytes[bytes.length - 2];
    //   double w = i / 200.0;

    //   weight = w.toString();
    // }
    return weight;
  }
}
